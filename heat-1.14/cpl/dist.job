#!/bin/bash
#$ -pe openmpi 8
#$ -l h_rt=0:15:00
#$ -N heatjob
#$ -cwd

APP=./heat_multi
ARGS="-nl 1"

# Get OpenMPI settings
. /etc/bashrc

SSH_SERVERS=`uniq $TMPDIR/machines  | tr '\n' ' '`
PATH=/usr/bin:$PATH

CHPL_COMM=gasnet SSH_SERVERS="$SSH_SERVERS" $APP $ARGS
